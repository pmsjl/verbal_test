# 后端 API 基地址
#
# Vite 加载优先级：
#   - 本机开发 (pnpm dev)：读取 .env / .env.development
#   - 生产构建 (pnpm build)：读取 .env / .env.production
#
# 默认 .env 内容是本机开发场景：
VITE_API_BASE=http://localhost:8080

# 生产构建场景请改 .env.production，把上面这行替换成真实后端域名，例如：
# VITE_API_BASE=https://api.verbal.example.com
